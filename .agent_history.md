# Histórico do agente (pipeline ISS)

## 2026-05-14 — Orquestrador: integração de VTTS sem `lessons.json`

### Contexto

- `documentation.md` como SSOT.
- `agents/discipline-map.yaml` criado: pastas `downloads/<Disciplina>/` → `discipline_slug` em `content/disciplines.json`; regra `Aula_NN_-_*.vtt` ↔ `order: NN` por disciplina.
- Gaps detetados (VTT com `order` sem entrada): `python-processamento-dados` aulas 9–10; `sql-modelagem-relacional` aula 7.

### Ficheiros alterados / criados

| Ficheiro | Ação |
|---------|------|
| `agents/discipline-map.yaml` | criado |
| `content/python-processamento-dados/aula-09-conjuntos-sets-operacoes-colecoes.md` | criado |
| `content/python-processamento-dados/aula-10-comparacao-duas-listas-intersecao-diferenca-uniao.md` | criado |
| `content/sql-modelagem-relacional/aula-07-caso-integrado-cinemas-requisitos-ao-sql.md` | criado |
| `content/lessons.json` | +3 entradas |
| `content/search-index.json` | +3 `excerpt` |

### Slugs publicados

- `python-processamento-dados` / `conjuntos-sets-operacoes-colecoes` — `order` 9  
- `python-processamento-dados` / `comparacao-duas-listas-intersecao-diferenca-uniao` — `order` 10  
- `sql-modelagem-relacional` / `caso-integrado-cinemas-requisitos-ao-sql` — `order` 7  

### Verificação automática (JSON + ficheiros)

```bash
python3 - <<'PY'
import json, os
from collections import Counter
with open("content/lessons.json") as f: lessons = json.load(f)
pairs = [(L["discipline"], L["slug"]) for L in lessons]
assert not [p for p,c in Counter(pairs).items() if c>1]
for L in lessons:
    assert os.path.isfile(os.path.join("content", L["file"]))
print("OK", len(lessons))
PY
```

Resultado: `OK 80` (sem pares duplicados; todos os `file` existem).

### Smoke manual (conforme `documentation.md`)

Com servidor estático na raiz do repositório (`python -m http.server 8765`):

1. URLs de aula (query `d` e `a`):
   - `http://127.0.0.1:8765/public/aula.html?d=python-processamento-dados&a=conjuntos-sets-operacoes-colecoes`
   - `http://127.0.0.1:8765/public/aula.html?d=python-processamento-dados&a=comparacao-duas-listas-intersecao-diferenca-uniao`
   - `http://127.0.0.1:8765/public/aula.html?d=sql-modelagem-relacional&a=caso-integrado-cinemas-requisitos-ao-sql`
2. Home / pesquisa: após atualizar `content/search-index.json`, validar na `index.html` termos como **conjuntos**, **interseção**, **caso integrado cinemas** nos resultados.

### Fontes primárias (VTT)

- `downloads/Python_para_Processamento_de_Dados/Aula_09_-_11052026.vtt` (sha256 `35b9f5557659dedd4b69d55b8240a6329b328b0b061cf3d23207b2b790dc4ad8`)
- `downloads/Python_para_Processamento_de_Dados/Aula_10_-_13052026.vtt` (sha256 `187ca755e962638f7c3206ffe7ab59f269471e330320bf83adc71d3e20a85fdf`)
- `downloads/SQL_e_Modelagem_Relacional/Aula_07_-_12052026.vtt` (sha256 `bde8a7808a14e53eb70684721ce961ffdea1dbcd451e06ee72c9f22986c9d7ad`)

### Smoke HTTP (servidor local `python -m http.server 8765`)

- `GET /content/lessons.json` → 200  
- `GET /content/python-processamento-dados/aula-09-conjuntos-sets-operacoes-colecoes.md` → 200  
- `GET /public/aula.html?d=python-processamento-dados&a=conjuntos-sets-operacoes-colecoes` → 200  

### PDA

- Redação e integração JSON desta leva em série pelo orquestrador (sem filhos paralelos em `lessons.json`).
