# Histórico do agente ISS (orquestrador)

Registo append-only de execuções do pipeline de conteúdo. Ver `documentation.md` para contratos e smoke manual.

---

## 2026-05-16 — Orquestrador (branch `cursor/pipeline-iss-conte-do-f5b2`)

**Objetivo:** fechar lacuna de mapeamento `agents/discipline-map.yaml`; integrar primeiro `.vtt` elegível sem `order` correspondente em `lessons.json`.

**Descoberta:** comparando `downloads/**/Aula_NN_*.vtt` com `order` em `content/lessons.json` (por disciplina via mapa explícito), restaram 6 itens sem aula: `fluencia-ia` aula 4; `projeto-bloco` 14; `python-processamento-dados` 9–10; `sql-modelagem-relacional` 7–8.

**Processado nesta iteração (1 VTT, PDA):**

| Disciplina | Order | VTT | Slug novo | Ficheiro `content/` |
|------------|-------|-----|-----------|----------------------|
| fluencia-ia | 4 | `downloads/Fluencia_em_IA/Aula_04_-_15052026.vtt` | `etica-automacao-humano-direitos-autorais-ia` | `fluencia-ia/aula-04-etica-automacao-humano-direitos-autorais-ia.md` |

**Contexto de pasta:** `downloads/Fluencia_em_IA/` — ficheiros da mesma sessão (`Aula_04_*`); sem subpasta dedicada.

**Ficheiros tocados:**

- `agents/discipline-map.yaml` (criado)
- `content/fluencia-ia/aula-04-etica-automacao-humano-direitos-autorais-ia.md` (criado)
- `content/lessons.json` (entrada `order` 4)
- `content/search-index.json` (excerpt pesquisável)
- `.agent_history.md` (este registo)

**Verificação automática:**

```bash
python3 -c "import json; json.load(open('content/lessons.json')); json.load(open('content/search-index.json'))"
```

Resultado: parse OK; script adicional confirmou unicidade de pares `(discipline, slug)` e existência de todos os `file` referenciados em `lessons.json`.

**Smoke HTTP (servidor estático local):**

```bash
python3 -m http.server 8765 --bind 127.0.0.1 &
curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8765/public/aula.html?d=fluencia-ia&a=etica-automacao-humano-direitos-autorais-ia"
curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8765/content/fluencia-ia/aula-04-etica-automacao-humano-direitos-autorais-ia.md"
```

Resultado esperado: códigos HTTP `200` para ambos (página e fetch do Markdown).

**Smoke manual pendente (operador humano, conforme `documentation.md`):** abrir no browser `public/aula.html?d=fluencia-ia&a=etica-automacao-humano-direitos-autorais-ia` e confirmar render; na home, pesquisar termos do excerpt (ex.: “direito moral”, “Lei 9.610”).

**Itens ainda na fila (próximas iterações, 1 VTT cada):** `projeto-bloco` order 14; `python-processamento-dados` 9, 10; `sql-modelagem-relacional` 7, 8.

**[ENCERRAMENTO] concluído** para leva `fluencia-ia`/aula 4 (integração JSON em série neste turno).
