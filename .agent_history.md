# Histórico do agente ISS (orquestrador)

## 2026-05-15 — Pipeline ISS (descoberta VTT vs `lessons.json`)

### Contexto

- SSOT: `documentation.md` (raiz).
- Manifesto de mapeamento criado: `agents/discipline-map.yaml` (antes inexistente — bloqueio removido).
- Lacuna detetada: pasta `downloads/SQL_e_Modelagem_Relacional/` com **8** ficheiros `.vtt` e disciplina `sql-modelagem-relacional` com apenas **6** entradas em `content/lessons.json` (até `order` 6).

### Ficheiros alterados / criados

| Caminho | Ação |
|---------|------|
| `agents/discipline-map.yaml` | Criado — mapeamento `download_folder` → `discipline` |
| `content/sql-modelagem-relacional/aula-07-revisao-etapas-mer-der-cinema-sql.md` | Criado |
| `content/sql-modelagem-relacional/aula-08-etapa-5-sqlite-studio-tp2-der.md` | Criado |
| `content/lessons.json` | Inseridas aulas `order` 7 e 8 (`revisao-etapas-mer-der-cinema-sql`, `etapa-5-sqlite-studio-tp2-der`) |
| `content/search-index.json` | Entradas de pesquisa para os dois slugs |
| `.agent_history.md` | Este registo |

### Fontes VTT (hashes SHA-256)

- `downloads/SQL_e_Modelagem_Relacional/Aula_07_-_12052026.vtt` → `bde8a7808a14e53eb70684721ce961ffdea1dbcd451e06ee72c9f22986c9d7ad`
- `downloads/SQL_e_Modelagem_Relacional/Aula_08_-_14052026.vtt` → `999594e1c50b0d7f246db0270079cab4fd3aa34be15198618de38665a6c96ccb`

### Escolha de contexto auxiliar

- Pastas de download por disciplina **planas** (sem subpasta por aula): contexto = apenas o `.vtt` da sessão; documentos em `downloads/documents/` não foram anexados por não haver subpasta explícita da aula no manifesto alinhada a 07/08.

### Verificação automática (gate Fase 3)

Comando:

```bash
node -e "const fs=require('fs');const lessons=JSON.parse(fs.readFileSync('content/lessons.json','utf8'));const pairs=new Set();for(const l of lessons){const k=l.discipline+'\0'+l.slug;if(pairs.has(k))throw new Error('dup '+k);pairs.add(k);require('fs').accessSync('content/'+l.file);} JSON.parse(fs.readFileSync('content/search-index.json','utf8')); console.log('OK');"
```

Resultado: `OK` (parse JSON, unicidade `(discipline, slug)`, ficheiros `file` existentes).

### Smoke manual (servidor estático)

Comando (servidor temporário na raiz do repo, porta 8765):

```bash
python3 -m http.server 8765
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/content/lessons.json
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/content/sql-modelagem-relacional/aula-07-revisao-etapas-mer-der-cinema-sql.md
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/content/sql-modelagem-relacional/aula-08-etapa-5-sqlite-studio-tp2-der.md
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/public/aula.html
```

Resultado esperado: `200` em todos (confirmado na execução do agente).

### URLs de leitura humana (checklist `documentation.md`)

- `public/aula.html?d=sql-modelagem-relacional&a=revisao-etapas-mer-der-cinema-sql`
- `public/aula.html?d=sql-modelagem-relacional&a=etapa-5-sqlite-studio-tp2-der`

### PDA

- Fan-out a agentes filhos: **não utilizado** nesta leva (orquestrador integrou `.md` + JSON em série).
