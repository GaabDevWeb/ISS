# Histórico do agente (pipeline ISS)

Registo append-only de execuções do orquestrador / integração de conteúdo.

---

## 2026-05-17 — Integração aula 14 Projeto de Bloco (VTT 15/05/2026)

**Contexto de pasta (downloads):** `downloads/Projeto_de_Bloco_Fundamentos_do_Processamento_de_Dados/` — ficheiros da mesma sessão: `Aula_14_-_15052026.vtt` (fonte primária), `Aula_14_-_15052026.md` (metadados mínimos + referência ao `.vtt`).

**Mapeamento:** `agents/discipline-map.yaml` → `discipline: projeto-bloco`, regra `lesson_order_from: aula_number_in_filename` (número em `Aula_NN_...` alinha com `order` em `content/lessons.json`).

**Descoberta:** único `.vtt` com número de aula sem entrada correspondente em `lessons.json` na disciplina mapeada: **Aula 14** (existiam ordens 1–13; faltava 14).

**Ficheiros alterados / criados:**

- `agents/discipline-map.yaml` (novo)
- `content/projeto-bloco/aula-14-relacionamentos-n-n-views-app-python-fila-atendimento.md` (novo)
- `content/lessons.json` — entrada `order` 14, `slug` `relacionamentos-n-n-views-app-python-fila-atendimento`
- `content/search-index.json` — excerpt para o par `projeto-bloco/relacionamentos-n-n-views-app-python-fila-atendimento`

**Verificação (JSON + ficheiros + unicidade):**

```bash
cd /workspace && python3 - <<'PY'
import json
from pathlib import Path
from collections import Counter
base = Path("content")
lessons = json.loads((base / "lessons.json").read_text(encoding="utf-8"))
idx = json.loads((base / "search-index.json").read_text(encoding="utf-8"))
pairs = [(x["discipline"], x["slug"]) for x in lessons]
dup = [p for p, c in Counter(pairs).items() if c > 1]
assert not dup, dup
for row in lessons:
    rel = base / row["file"]
    assert rel.is_file(), row["file"]
keys_si = {(x["discipline"], x["slug"]) for x in idx}
# search-index pode não cobrir todas as aulas; só garantimos parse e que a nova chave existe
assert ("projeto-bloco", "relacionamentos-n-n-views-app-python-fila-atendimento") in keys_si
print("OK lessons:", len(lessons), "search rows:", len(idx))
PY
```

**Resultado:** `OK lessons: 78 search rows: 25` (execução neste workspace).

**Smoke manual (documentation.md):** após servir estáticos na raiz do repo, abrir `public/aula.html?d=projeto-bloco&a=relacionamentos-n-n-views-app-python-fila-atendimento` e na home confirmar pesquisa por termos do excerpt (ex.: “N para N”, “fila de atendimento”).
