# Agent history — ISS

## Job 4 — verify-kernelbot-sync (2026-05-19)

- **Script:** `.github/scripts/verify-kernelbot-sync.mjs` — tripla verificação: `validate-report.json` (SSOT), `COUNT(DISTINCT discipline, slug)` no MySQL (critério ingest), `GET /health/catalog` com Bearer.
- **Workflow:** job `verify` em `sync-kernelbot-knowledge.yml` — `needs: [validate, reload]`, download de artefatos em `.github/reports/`, upload `verify-report.json`.
- **reload-kernelbot.mjs:** comentário no topo (getReader + TextDecoder, sem `setEncoding`); flush opcional do decoder após o loop SSE.
- **Verificação local:** `node --check .github/scripts/verify-kernelbot-sync.mjs` e `reload-kernelbot.mjs`.

## Fase 5b documentada (2026-05-19)

- **Doc:** `documentation.md` — secção «Pipeline de Sincronização e Ingestão Automática (Fase 5b)» (Jobs J1–J5, mermaid, secrets, rede GHA, BM25, sem auto-heal).
- **Agentes:** `agents/documentation.md` — pointer para SSOT na raiz.
- continuar

## Fase 5b chunks enriquecidos (2026-05-20)

- **Script:** `.github/scripts/ingest-knowledge.py` — J2 passa a ler `jsons/{discipline}/{discipline}__NN__{slug}.json` (não `.md`); cabeçalho `[CONTEXTO DA AULA]` + chunking 500/50 (espelha `KernelBot/engine/database.py`); chunks concatenados com `\n\n` no campo MySQL `content`.
- **Risco aceite:** chunking duplo no runtime do KernelBot permanece até alinhar `fetch_db_chunks`.
- continuar
