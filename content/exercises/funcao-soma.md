---
title: Função soma
slug: funcao-soma
concepts: functions
difficulty: easy
discipline: python
editor: true

starter_code: |
  def soma(a, b):
      pass

tests:
  - input: [2, 3]
    expected: 5
  - input: [10, 5]
    expected: 15
  - input: [0, 0]
    expected: 0
---

## Enunciado

Implemente a função `soma(a, b)` que recebe dois números e retorna a soma deles.

Exemplos:
- `soma(2, 3)` deve retornar `5`
- `soma(10, 5)` deve retornar `15`

Use o editor abaixo para escrever seu código e clique em **Verificar solução** para rodar os testes automáticos.
