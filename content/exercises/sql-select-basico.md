---
title: "SELECT básico — listar tabela"
slug: "sql-select-basico"
difficulty: "easy"
concepts: "SELECT, SQL, consulta"
discipline: "visualizacao-sql"
language: "sql"
---

## Enunciado

Escreva uma consulta SQL que retorne todas as colunas de uma tabela chamada `clientes`.

Use a forma mais simples de `SELECT` para listar todos os registros da tabela.

## Solução

```sql
SELECT * FROM clientes;
```
